/*
(Data Type) (Name) (Value)
Data types: 
    Integers-
        Char
        Unsigned Char >=0
        Short
        Unsigned Short >=0
        Int
        Unsigned Int >=0
        Long
        Unsigned Long >=0
        Long Long
        Unsigned Long Long >=0
    Decimal Data Types-
        Float
        Decimal?
    Strings- (Standard Library)
        std::string "String" 

Name = *
Value = Amount (number)
*/

#include <iostream>

using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}

// Need to build/compile the program first
// Next run the file
// g++ compiles the program
// "g++ -o first first.cpp" (This will name the executable program first)
// ./first (This will run the file)
// **rm will remove a file (in Unix)